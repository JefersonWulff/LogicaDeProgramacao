#include <stdio.h>
 
int main() {
    int NUMBER, HOURS;
    double SALARY, VALOR;
    
    scanf("%d", &NUMBER);
    printf("NUMBER = %d\n", NUMBER);
    
    scanf("%d %lf", &HOURS, &VALOR);
    
    SALARY = HOURS * VALOR;
    
    printf("SALARY = U$ %.2lf\n", SALARY);

    return 0;
}
